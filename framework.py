"""
Functions we need:

invoke
query info: remote_qp_num
            p_key
            q_key
            which qp's are taken on a given node
instantiate a qp (or all 3 on a given node)

No scheduler, probably, just keep it simple
"""